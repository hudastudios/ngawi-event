{
  "name": "project-dispora",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --workspace=web",
    "vercel-build": "npm run build --workspace=web && mkdir -p dist && cp -r apps/web/dist/* dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*"
  ],
  "type": "commonjs"
}